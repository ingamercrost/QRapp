import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'CL.DUOCQR',
  appName: 'DuocQR',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
